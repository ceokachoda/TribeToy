const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { error } = await supabase.rpc('execute_sql', { sql: 'ALTER TABLE products ADD COLUMN IF NOT EXISTS additional_images text[] DEFAULT ''{}''::text[];' }); if (error) { console.error('RPC failed:', error); const { error: e2 } = await supabase.from('products').select('*').limit(1); if(!e2) console.log('You need to run this in Supabase dashboard manually, or use an existing tool. Wait, there is no execute_sql unless I made one. I will use the Supabase MCP to apply migrations.'); } else { console.log('Success!'); } } run();  
